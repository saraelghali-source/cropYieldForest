% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_features.R
\name{preprocess_data}
\alias{preprocess_data}
\title{Prétraitement du jeu de données pour le Machine Learning}
\usage{
preprocess_data(
  features,
  target = "yield",
  exclude_cols = c("field_id", "date", "crop", "geometry"),
  test_ratio = 0.25,
  seed = 42,
  impute_method = "median",
  scale = FALSE
)
}
\arguments{
\item{features}{data.frame issu de \code{extract_features}.}

\item{target}{Nom de la colonne cible (défaut: "yield").}

\item{exclude_cols}{Colonnes à exclure du modèle (ex: identifiants).}

\item{test_ratio}{Proportion pour le jeu de test (défaut: 0.25).}

\item{seed}{Graine aléatoire pour la reproductibilité (défaut: 42).}

\item{impute_method}{Méthode d'imputation des NA numériques :
"median" (défaut) ou "mean".}

\item{scale}{Centrer et réduire les variables numériques ? (défaut: FALSE).}
}
\value{
Liste avec :
\describe{
\item{train}{data.frame d'entraînement.}
\item{test}{data.frame de test.}
\item{features_used}{Noms des variables explicatives retenues.}
\item{n_train}{Nombre d'observations d'entraînement.}
\item{n_test}{Nombre d'observations de test.}
}
}
\description{
Nettoie le dataset de features : supprime les doublons, impute les NA,
encode les variables catégorielles et sépare les données en jeux
d'entraînement et de test.
}
\examples{
\dontrun{
features <- extract_features(...)
data_ml  <- preprocess_data(features, target = "yield")
cat("Train:", data_ml$n_train, "| Test:", data_ml$n_test)
}

}
