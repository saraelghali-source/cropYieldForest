% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_outputs.R
\name{predict_yield_map}
\alias{predict_yield_map}
\title{Prédiction spatiale du rendement sur raster}
\usage{
predict_yield_map(
  model_result,
  ndvi_stack = NULL,
  indices_stack = NULL,
  weather_df = NULL,
  features_used = NULL,
  out_dir = "outputs/"
)
}
\arguments{
\item{model_result}{Liste issue de \code{train_rf_model}.}

\item{ndvi_stack}{SpatRaster NDVI utilisé pour la prédiction spatiale.}

\item{indices_stack}{SpatRaster des autres indices (SAVI, EVI...). NULL par défaut.}

\item{weather_df}{data.frame météo (sera assigné uniformément à chaque pixel).}

\item{features_used}{Vecteur des noms de variables du modèle
(model_result$features_used ou data_ml$features_used).}

\item{out_dir}{Répertoire de sortie (défaut: "outputs/").}
}
\value{
SpatRaster du rendement prédit (t/ha).
}
\description{
Applique le modèle Random Forest entraîné à l'ensemble du raster
de la zone d'étude pour générer une carte continue de rendement prédit.
Exporte le résultat en GeoTIFF.
}
\examples{
\dontrun{
yield_map <- predict_yield_map(
  model_result  = model,
  ndvi_stack    = veg$ndvi,
  weather_df    = meteo,
  features_used = data_ml$features_used
)
terra::plot(yield_map, main = "Rendement prédit (t/ha)")
}

}
