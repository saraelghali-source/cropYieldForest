% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_features.R
\name{extract_features}
\alias{extract_features}
\title{Extraction des variables par parcelle}
\usage{
extract_features(
  fields,
  ndvi_stack = NULL,
  indices_stack = NULL,
  weather_df = NULL,
  fun = c("mean", "max", "min", "sd"),
  method = "exact"
)
}
\arguments{
\item{fields}{Objet \code{sf} des parcelles (sortie de \code{import_field_data}).}

\item{ndvi_stack}{SpatRaster NDVI (une ou plusieurs dates).}

\item{indices_stack}{SpatRaster des indices supplémentaires (SAVI, EVI...).
NULL par défaut.}

\item{weather_df}{data.frame météo (sortie de \code{import_weather_data}).
NULL par défaut. Si fourni, on agrège sur la saison culturale de chaque
parcelle (colonne "date" dans fields).}

\item{fun}{Fonctions statistiques zonales à calculer (défaut: mean, max, min, sd).}

\item{method}{Méthode d'extraction : "exact" (exactextractr, recommandé)
ou "centroid" (terra::extract sur le centroïde).}
}
\value{
data.frame avec une ligne par parcelle et les colonnes :
field_id, yield (si présent), ndvi_mean, ndvi_max, ndvi_min, ndvi_sd,
\itemize{
\item statistiques des autres indices, + variables météo si fournies.
}
}
\description{
Extrait les statistiques zonales des rasters d'indices de végétation
(NDVI moyen, max, min, écart-type) et joint les données météorologiques
pour chaque parcelle. Produit le jeu de données prêt pour le Machine Learning.
}
\examples{
\dontrun{
fields <- import_field_data("parcelles.shp")
veg    <- download_sentinel(lat = 34.5, lon = -6.2,
                            start_date = "2022-04-01", end_date = "2022-06-30")
meteo  <- import_weather_data(lat = 34.5, lon = -6.2,
                              start_date = "2022-01-01", end_date = "2022-12-31",
                              aggregate = "monthly")
features <- extract_features(
  fields        = fields$sf_object,
  ndvi_stack    = veg$ndvi,
  weather_df    = meteo
)
head(features)
}

}
