% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_field_data.R
\name{import_field_data}
\alias{import_field_data}
\title{Import des données parcellaires}
\usage{
import_field_data(
  path,
  yield_col = "yield",
  crop_col = "crop",
  date_col = "date",
  crs = 4326,
  remove_na = TRUE
)
}
\arguments{
\item{path}{Chemin vers le fichier de données (CSV, .shp ou .geojson).}

\item{yield_col}{Nom de la colonne rendement (défaut: "yield").}

\item{crop_col}{Nom de la colonne culture (défaut: "crop").}

\item{date_col}{Nom de la colonne date (défaut: "date").}

\item{crs}{Système de coordonnées souhaité, code EPSG (défaut: 4326 = WGS84).}

\item{remove_na}{Supprimer les lignes avec rendement NA ? (défaut: TRUE).}
}
\value{
Liste avec deux éléments :
\describe{
\item{sf_object}{Objet \code{sf} avec géométrie des parcelles.}
\item{dataframe}{data.frame nettoyé sans géométrie.}
}
}
\description{
Importe et nettoie les données de terrain (parcelles agricoles).
Supporte les formats CSV, Shapefile et GeoJSON.
Les variables attendues sont le rendement, la culture, la date
et les pratiques agricoles.
}
\examples{
\dontrun{
# Depuis un CSV avec coordonnées lon/lat
fields <- import_field_data(
  path      = "inst/field_data/parcelles.csv",
  yield_col = "rendement_t_ha",
  crop_col  = "culture",
  date_col  = "date_recolte"
)
head(fields$dataframe)
plot(fields$sf_object["yield"])
}

}
